

*1) Для практикума из занятия 7 продумать, где можно применить замыкания

2) Не выполняя кода, ответить, что выведет браузер и почему

if (!("a" in window)) {
    var a = 1;
}
alert(a); //Результатом будет undefined, так как переменная "а" объявляется локально, глобально она не определена.


var b = function a(x) {
    x && a(--x);
};
alert(a);//Результатом будет undefined, так как результат выполнения функции присвоен переменной "b", а выводится в браузер значение переменной "а", которая не определена


function a(x) {
    return x * 2;
}
var a;
alert(a);//	Результатом будет:	function a(x) { пока не разобрался, но что-то связанное с дублированием.
    			return x * 2;
			}


function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);//Результатом будет 10 , т.к. функция вызывается с переданным ей аргументом, который после ее вызова перезаписывается и выводится в браузер


function a() {
    alert(this);
}//Результатом будетльного объекта window. Не смог разобраться
a.call(null);//Выведет имя гло