3. Объяснить, как работает данный код:
<?php
    $a = 5;
    $b = '05';
1).    var_dump($a == $b);         // Почему true?
    2).var_dump((int)'012345');     // Почему 12345?
      3).var_dump((float)123.0 === (int)123.0); // Почему false?
        4).var_dump((int)0 === (int)'hello, world'); // Почему true?
?>

5. *Используя только две переменные, поменяйте их значение местами. Например, если a = 1, b = 2, надо, чтобы получилось: b = 1, a = 2. Дополнительные переменные использовать нельзя.
b-=a; //b=1;
a+=b;//a=2;


				Ответ.
Функция var_dump позволяет вывести тип переменной и её значение(Н: echo var_dump($a == $b) выведет=> bool(true) ).

	1).Если происходит сравнение числа со строкой, то строка будет преобразована в число. Это
	      обусловлено механизмом преобразования типов, встроенного в интерпретатор.
		результат: bool(true);
	     

	2). Ответа не нашел. Думаю, что при преобразовании строки в число






	3). В этом примере используется оператор строгого равенства. Т.е. сравнивается не только значение, но и тип данных.
	      Поэтому результатом сравнения двух переменных с разными типами данных будет false.


	4).В этом примере мы явно преобразовываем строку к целому числу и результатом при преобразовании будет int()0.
Поэтому результатом строгого равенства будет true.